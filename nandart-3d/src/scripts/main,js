import * as THREE from 'three';

// Função de inicialização
function init() {
  // Criação da cena
  const scene = new THREE.Scene();
  const camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000);
  const renderer = new THREE.WebGLRenderer();
  renderer.setSize(window.innerWidth, window.innerHeight);
  document.body.appendChild(renderer.domElement);

  // Adicionar iluminação
  const light = new THREE.AmbientLight(0x404040, 2); // luz ambiente suave
  scene.add(light);

  const pointLight = new THREE.PointLight(0xffffff, 1, 100);
  pointLight.position.set(5, 5, 5);
  scene.add(pointLight);

  // Adicionar chão
  const floorGeometry = new THREE.PlaneGeometry(100, 100);
  const floorMaterial = new THREE.MeshBasicMaterial({ color: 0x222222, side: THREE.DoubleSide });
  const floor = new THREE.Mesh(floorGeometry, floorMaterial);
  floor.rotation.x = Math.PI / -2;
  scene.add(floor);

  // Adicionar cubos (obras de arte) no centro
  const geometry = new THREE.BoxGeometry(1, 1, 1);
  const material = new THREE.MeshBasicMaterial({ color: 0x00ff00 });
  const obra = new THREE.Mesh(geometry, material);
  obra.position.set(0, 0.5, 0);
  scene.add(obra);

  // Posicionar a câmera
  camera.position.z = 5;

  // Função de animação
  function animate() {
    requestAnimationFrame(animate);
    obra.rotation.x += 0.01;
    obra.rotation.y += 0.01;
    renderer.render(scene, camera);
  }

  animate();
}

init();
