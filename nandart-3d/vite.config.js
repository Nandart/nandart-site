// vite.config.js
export default {
  root: '.',              // Usa a raiz do projeto
  build: {
    outDir: 'dist',       // Onde o Vercel vai procurar os ficheiros gerados
    emptyOutDir: true
  }
}

