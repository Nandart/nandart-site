{
  "name": "nandart-3d",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",            // Comando para rodar em ambiente de desenvolvimento
    "build": "vite build",    // Comando para construir o projeto para produção
    "preview": "vite preview" // Comando para visualizar a versão construída
  },
  "dependencies": {
    "three": "^0.137.5",      // Dependência do Three.js (necessária para 3D)
    "vite": "^4.0.0"          // Dependência do Vite (bundler para o projeto)
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^3.0.0"  // Dependência do plugin Vue, se necessário
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "license": "MIT",
  "description": "3D Art Gallery Project"
}
